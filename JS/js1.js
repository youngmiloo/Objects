// exercise 1

// a)
let user = {};

// b)
user.name = "John";

// c)
user.surname = "Smith";

// d)
user.name = "Pete";

// e)
delete user.name;

// f)
function isEmpty(obj) {
  return Object.keys(obj).length === 0;
}